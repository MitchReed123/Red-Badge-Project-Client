//not available to userRole = "User", holds update and delete, just for "Admin"
// grabbing from Destination Table, http://localhost:3000/:id, PUT(UPDATE)
import React from "react";
export default class DestinationUpdate extends React.Component {
  render() {
    return (
      <div>
        <h1>Destination Update</h1>
      </div>
    );
  }
}
